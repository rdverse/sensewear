# sensewear

Application to stream accelerometer data on an android smartwatch.

## Steps

- Accelerometer samples at 50Hz.
- Mean for every second is calculated.
- Creates a csv file every 5 minutes and upload to firebase drive (A local copy is also saved).

## Limitations

- The rate at which files are saved is hardcoded.
- Sampling rate is hardcoded.

